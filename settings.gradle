
pluginManagement {
  repositories {
    mavenLocal()
    gradlePluginPortal()
    maven { url 'https://dl.bintray.com/brambolt/public' }
  }
  plugins {
    id 'com.brambolt.gradle.build.library' version bramboltVersion
  }
}

rootProject.name = 'rt-jetty'
